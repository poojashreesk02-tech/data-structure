#include<stdio.h>
#define MAX 3
int stack[MAX];
int top=-1;
void pop(){
    if(top==-1)
    printf("stack is overflow\n");
    else{
        printf("%d poped\n",stack[top]);
    }
    
}
int main(){
    pop(3);
    pop(2);
    pop(1);
}
