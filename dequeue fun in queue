#include<stdio.h>
#define SIZE 5
int queue[SIZE];
int front=-1, rear=-1;
void dequeue() {
    if (front == -1 || front > rear)
        printf("Queue is Empty\n");
    else {
        printf("%d deleted\n", queue[front]);
        front++;
    }
}

int main(){
    dequeue();
    
 
}
